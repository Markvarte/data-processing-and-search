
def find_score(selected_postings_dict : dict, join_postings_list : list):
    # selected_postings_dict - dictionary, keys = terms, vals = postings for terms
    # join_postings_list - all distinct postings for one query terms
    term_count_in_doc(selected_postings_dict.keys())
    return ['empty']

def term_count_in_doc(terms):
    term_freq = 0

    return term_freq


def TF_per_term(term, postings):
    TF = 0

    return TF
